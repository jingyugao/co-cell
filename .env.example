# Optional. With no API key, uses the local Codex login/configuration.
# CODEX_API_KEY=
# OPENAI_API_KEY=
# OPENAI_BASE_URL=
# CODEX_PATH=/absolute/path/to/codex
# CODEX_MODEL=
# CODEX_WORKSPACE=/absolute/path/to/project
# CODEX_WEB_DATA_DIR=/absolute/path/to/private/session-storage
PORT=3001
# Optional SDK config object / raw TOML overrides, applied on top of local config.
# CODEX_CONFIG_JSON={"features":{"multi_agent":true}}
# CODEX_CONFIG_OVERRIDES_JSON=[]
# OPENAI_BASE_URL registers a custom codex_web_proxy provider using HTTP streaming.
# The E2B diagnostics/retry proxy requires HTTP streaming (not WebSocket).

# E2B: by default uses the local service from ~/.data/e2b/client/demo.mjs.
# New sessions default to E2B when its key and a model API key are available.
# With E2B enabled, local execution is disabled and local history is hidden.
# Each project owns a sandbox; its sessions share that sandbox and workspace.
# Codex has full access inside E2B; shared AGENTS.md provides workspace guidance.
# Model credentials are never returned to the browser.
# CODEX_EXECUTION_MODE=e2b
# E2B_ENABLED=true
# E2B_API_KEY_FILE=/home/your-user/.data/e2b/config/api-key
# E2B_API_KEY=
# E2B_API_URL=http://127.0.0.1:13000
# E2B_SANDBOX_URL=http://127.0.0.1:13002
# E2B_DOMAIN=localhost
# E2B_TEMPLATE=base
# After pnpm e2b:build succeeds, use E2B_TEMPLATE=codex-dev for preinstalled toolchains.
# A default selected in the template management page persists and takes precedence.
# E2B_WORKSPACE=/home/user/workspace
# Idle scan runs every minute after >1 hour inactivity; leases renew separately (5–60 minutes).
# E2B_TIMEOUT_MS=1800000
